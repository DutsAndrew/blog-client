import styles from '../page.module.css';

const Sidebar = () => {
  return (
    <section className={styles.sidebarContainer}>
      <p>
        Sidebar
      </p>
    </section>
  );
};

export default Sidebar;